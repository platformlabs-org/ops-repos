# Troubleshooting

## Authentication Failed
Check `MS_CLIENT_ID`, `MS_CLIENT_SECRET`, `MS_TENANT_ID` secrets.

## Download Failed
Check if Submission ID is valid and accessible.

## HLKX Not Found
Ensure a previous step has uploaded an `.hlkx` file as an attachment to a comment.
