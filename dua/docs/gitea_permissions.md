# Gitea Permissions

The workflow requires:
*   `issues: write` (to post comments)
*   `contents: read` (to read code)
*   `actions: read`

Runner requires network access to:
*   Gitea instance
*   `login.microsoftonline.com`
*   `api.partner.microsoft.com`
